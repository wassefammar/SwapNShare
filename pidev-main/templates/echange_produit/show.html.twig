{% extends "baseFrontOffice.html.twig" %}

{% block title %} Show  Product Exchange {% endblock %}

{% block body %}
    <h1>EchangeProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ echange_produit.id }}</td>
            </tr>
            <tr>
                <th>Date_echange</th>
                <td>{{ echange_produit.dateEchange ? echange_produit.dateEchange|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Valide</th>
                <td>{{ echange_produit.valide ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_echange_produit_index') }}">back to list</a>

    <a href="{{ path('app_echange_produit_edit', {'id': echange_produit.id}) }}">edit</a>

    {{ include('echange_produit/_delete_form.html.twig') }}
{% endblock %}
